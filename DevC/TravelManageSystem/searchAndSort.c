#include "myHeader.h"

travelItem* sortByID(travelItem * itemToBeSortHead)
{
    travelItem *Head, *temp1, *temp2;
    Head = temp1 = temp2 = NULL;
    if(itemToBeSortHead == NULL)return NULL;

    Head = itemToBeSortHead;
}